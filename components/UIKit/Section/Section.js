import styled from 'styled-components';
import {
  space,
  layout,
  flexbox,
  grid,
  color,
  position,
  border,
  typography,
  shadow,
} from 'styled-system';

const Section = styled.section`
  ${space}
  ${layout}
  ${flexbox}
  ${grid}
  ${color}
  ${position}
  ${border}
  ${typography}
  ${shadow}
`;

export default Section;
