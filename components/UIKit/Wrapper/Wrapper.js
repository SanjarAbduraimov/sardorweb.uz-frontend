import styled from 'styled-components';
import {
  color,
  space,
  layout,
  grid,
  position,
  border,
  shadow,
  flexbox,
  typography,
  background,
} from 'styled-system';

const Wrapper = styled.div`
  ${color}
  ${space}
  ${layout}
  ${grid}
  ${position}
  ${border}
  ${shadow}
  ${flexbox}
  ${typography}
`;

export default Wrapper;
