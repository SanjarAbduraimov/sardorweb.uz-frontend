import styled from 'styled-components';
import {
  color,
  space,
  layout,
  grid,
  typography,
  flexbox,
  border,
  position,
  shadow,
} from 'styled-system';

const Frame = styled.div`
  ${color}
  ${space}
  ${layout}
  ${grid}
  ${typography}
  ${flexbox}
  ${border}
  ${position}
  ${shadow}
`;

export default Frame;
