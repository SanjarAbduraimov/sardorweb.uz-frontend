import styled from 'styled-components';
import {
  space,
  layout,
  flexbox,
  grid,
  color,
  position,
  border,
  typography,
  shadow,
} from 'styled-system';

const Container = styled.div`
  ${space}
  ${layout}
  ${flexbox}
  ${grid}
  ${color}
  ${position}
  ${border}
  ${typography}
  ${shadow}
`;

export default Container;
